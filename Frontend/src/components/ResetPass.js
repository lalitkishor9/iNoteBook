import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { useSendEmail } from "../context/user/resetPass";
export default function ResetPass({ showAlert }) { const [form,setForm]=useState({email:"",code:"",password:""});const [sent,setSent]=useState(false);const [loading,setLoading]=useState(false);const {sendEmail,updatePassword}=useSendEmail();const navigate=useNavigate();const send=async e=>{e.preventDefault();setLoading(true);try{await sendEmail(form.email);setSent(true);showAlert("Check your inbox for the 6-digit code.");}catch(error){showAlert(error.message,"danger");}finally{setLoading(false)}};const reset=async e=>{e.preventDefault();setLoading(true);try{await updatePassword(form.email,form.password,form.code);showAlert("Password updated. You can now sign in.");navigate("/login");}catch(error){showAlert(error.message,"danger");}finally{setLoading(false)}};return <main className="page auth-page"><section className="auth-wrap"><aside className="auth-aside"><span className="eyebrow" style={{color:"#c9caff"}}>Account recovery</span><h1>Back to your ideas.</h1><p>We’ll send a time-limited code to your email so you can create a fresh password.</p></aside><section className="auth-card"><h2>{sent?"Set a new password":"Reset password"}</h2><p>{sent?"Enter the code from your inbox.":"We’ll email you a secure 6-digit code."}</p>{!sent?<form onSubmit={send}><div className="field"><label>Email address</label><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></div><button disabled={loading} className="button button-primary">{loading?"Sending…":"Send reset code"}</button></form>:<form onSubmit={reset}><div className="field"><label>Reset code</label><input inputMode="numeric" required minLength="6" maxLength="6" value={form.code} onChange={e=>setForm({...form,code:e.target.value})} placeholder="123456"/></div><div className="field"><label>New password</label><input type="password" required minLength="6" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 6 characters"/></div><button disabled={loading} className="button button-primary">{loading?"Updating…":"Update password"}</button></form>}<p className="auth-foot"><Link to="/login">Back to sign in</Link></p></section></section></main>}
