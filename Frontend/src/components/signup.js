import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import baseUrl from "../utils";
export function Signup({ showAlert }) { const [form,setForm]=useState({name:"",email:"",password:""}); const [loading,setLoading]=useState(false); const navigate=useNavigate(); const submit=async e=>{e.preventDefault();setLoading(true);try{const response=await fetch(`${baseUrl}/api/auth/createuser`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const data=await response.json();if(!response.ok)throw new Error(data.error||data.errors?.[0]?.msg||"Unable to create your account");localStorage.setItem("token",data.authToken);showAlert("Your notebook is ready.");navigate("/");}catch(error){showAlert(error.message,"danger");}finally{setLoading(false)}};return <main className="page auth-page"><section className="auth-wrap"><aside className="auth-aside"><span className="eyebrow" style={{color:"#c9caff"}}>A better notebook</span><h1>Start with a single thought.</h1><p>Capture ideas instantly, then return to them when you need them most.</p></aside><section className="auth-card"><h2>Create your space</h2><p>It takes less than a minute.</p><form onSubmit={submit}><div className="field"><label>Name</label><input required minLength="3" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name"/></div><div className="field"><label>Email address</label><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></div><div className="field"><label>Password</label><input required type="password" minLength="6" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 6 characters"/></div><button disabled={loading} className="button button-primary">{loading?"Creating…":"Create account"}</button></form><p className="auth-foot">Already have an account? <Link to="/login">Sign in</Link></p></section></section></main> }
