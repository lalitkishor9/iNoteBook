import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import baseUrl from "../utils";
export function Login({ showAlert }) { const [credentials,setCredentials]=useState({email:"",password:""}); const [loading,setLoading]=useState(false); const navigate=useNavigate(); const submit=async e=>{e.preventDefault();setLoading(true);try{const response=await fetch(`${baseUrl}/api/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(credentials)});const data=await response.json();if(!response.ok)throw new Error(data.error||"Unable to sign in");localStorage.setItem("token",data.authToken);showAlert("Welcome back.");navigate("/");}catch(error){showAlert(error.message,"danger");}finally{setLoading(false)}};return <main className="page auth-page"><section className="auth-wrap"><aside className="auth-aside"><span className="eyebrow" style={{color:"#c9caff"}}>Notely workspace</span><h1>Make room for your best ideas.</h1><p>A focused, private place for the small thoughts that grow into meaningful work.</p></aside><section className="auth-card"><h2>Welcome back</h2><p>Sign in to continue writing.</p><form onSubmit={submit}><div className="field"><label>Email address</label><input required type="email" value={credentials.email} onChange={e=>setCredentials({...credentials,email:e.target.value})} placeholder="you@example.com"/></div><div className="field"><label>Password</label><input required type="password" value={credentials.password} onChange={e=>setCredentials({...credentials,password:e.target.value})} placeholder="Your password"/></div><button disabled={loading} className="button button-primary">{loading?"Signing in…":"Sign in"}</button></form><p className="auth-foot"><Link to="/resetpassword">Forgot password?</Link> · New here? <Link to="/signup">Create an account</Link></p></section></section></main> }
